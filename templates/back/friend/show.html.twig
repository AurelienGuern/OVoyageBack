{% extends 'back/base_back.html.twig' %}

{% block title %}Ami.e{% endblock %}

{% block body %}
    <h1>Ami.e</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friend.id }}</td>
            </tr>
            <tr>
                <th>Relation</th>
                <td>{{ friend.relationship ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Créée par </th>
                <td>{{ friend.createdBy ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_friend_index') }}">Retour</a>

    <a href="{{ path('app_back_friend_edit', {'id': friend.id}) }}">Modifier</a>

    {{ include('back/friend/_delete_form.html.twig') }}
{% endblock %}
