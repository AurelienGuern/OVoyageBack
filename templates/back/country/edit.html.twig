{% extends 'back/base_back.html.twig' %}

{% block title %}Modifier {{country.name}}{% endblock %}

{% block body %}
    <h1>Modifier {{country.name}}</h1>

    {{ include('back/country/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_back_country_index') }}">Retour</a>

    {{ include('back/country/_delete_form.html.twig') }}
{% endblock %}
