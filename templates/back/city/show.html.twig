{% extends 'back/base_back.html.twig' %}

{% block title %}{{city.name}}{% endblock %}

{% block body %}
    <h1>Ville : {{city.name}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ city.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_city_index') }}">Retour</a>

    <a href="{{ path('app_back_city_edit', {'id': city.id}) }}">modifier</a>

    {{ include('back/city/_delete_form.html.twig') }}
{% endblock %}
