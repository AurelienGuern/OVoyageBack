{% extends 'back/base_back.html.twig' %}

{% block title %}Voyage : {{trip.name}} {% endblock %}

{% block body %}
    <h1> Voyage : {{trip.name}} </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ trip.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ trip.name }}</td>
            </tr>
            <tr>
                <th>Début</th>
                <td>{{ trip.startDate ? trip.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ trip.endDate ? trip.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ trip.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ trip.backgroundPicture }}</td>
            </tr>
    
        </tbody>
    </table>

    <a href="{{ path('app_back_trip_index') }}" class="btn btn-primary">Retour</a>

    <a href="{{ path('app_back_trip_edit', {'id': trip.id}) }}" class="btn btn-warning">Modifier</a>

    {{ include('back/trip/_delete_form.html.twig') }}
{% endblock %}
